# 三个工作入口

当前项目由三个相互独立、通过顶部导航互通的页面组成：

- `index.html`：设计台。保留 Three.js 展厅、FBX 导入、材质分配、图案贴合、场景灯光、项目 `.form` 保存/恢复和 PNG 导出。
- `material-editor.html`：材质编辑器。独立预览材质球或 100 × 100 cm 平面，编辑基础颜色、粗糙度、金属度、法线、AO、自发光、凹凸和 PBR 贴图，支持物理尺寸与材质包导出。
- `asset-library.html`：资产库。使用浏览器 IndexedDB 保存材质、模型和贴图，顶部标签分别显示面料、图案和模型；支持搜索、面料筛选及导入。4:3 卡片采用 8px 间距，模型提供“设计”，材质提供“编辑材质”；卡片菜单支持名称、简介、预览图管理及导出、删除。页面样式位于 `asset-library.css`，模型缩略图由 `shared/model-preview.js` 按队列生成。

## 现有代码结构

设计台原先集中在 `app.js`，它负责 Three.js 初始化、FBX 解析、材质转换、拖放替换、物理贴图、图案、标注、场景控制和 `.form` 项目存取。以下模块已经按职责拆开：

- `material-assignments.js`：模型网格的材质槽替换、使用计数、分配序列化与恢复。
- `physical-textures.js`：厘米尺寸、UV 度量和物理贴图矩阵。
- `image-density.js`：PNG/JPEG/BMP/WebP 的 DPI 读取与厘米换算。
- `uv-pattern.js`、`surface-pattern.js`：UV 图案与曲面图案几何。
- `material-reveal.js`：材质应用部位高亮动画。
- `shared/navigation.js`：三个入口的统一导航和浏览器下载。
- `shared/asset-store.js`：IndexedDB 资产持久化。
- `shared/material-data.js`：PBR 通道、面料分类和材质参数读写。
- `shared/material-package.js`：`.formmat` 材质包的打包、解包和输入校验。
- `entries/design.js`：设计台启动入口，显式调用 `app.js` 的 `startDesign()`。
- `entries/material-editor.js`、`entries/asset-library.js`：材质编辑器和资产库的页面入口逻辑。两者不导入 `app.js`，资产库不初始化 WebGL，也不加载展厅 FBX。

本次保留设计台已工作的模型交互和 `.form` 逻辑，抽出共用的资产/PBR 数据层。页面状态彼此独立；共享资产通过 IndexedDB 的 `spenic-workspace-assets/assets` 存储交换，URL 的 `asset` 参数用于定位资产。无构建工具和云端依赖。

材质资产保存表面参数、分类、物理尺寸、重复值、逐通道图片文件和 DPI 信息；原 FBX 内置贴图另保留 UV 变换。资产库仅作为可复用资源集合，不自动覆盖设计台正在编辑的模型。已有设计通过底栏 ＋ 弹窗添加库中材质，再用现有拖放操作应用。

`.formmat` 是含 `material.json` 和贴图的 ZIP；`.form` 继续保存完整 FBX 项目。旧项目仍可打开，项目恢复时补建新增的库中材质再恢复网格材质分配。

## 验证方式

- `node --test checks/*.test.mjs`：几何、DPI、材质分配、相机与材质包回归测试。
- 启动本地服务后执行 `node checks/entry-workflow.test.cjs`：浏览器完整流程测试。可通过 `PLAYWRIGHT_PATH` 指向已安装的 Playwright，通过 `FORM_BASE_URL` 指定本地服务地址，通过 `BROWSER_CHANNEL` 指定浏览器。
- 浏览器测试使用独立临时浏览器上下文，不修改日常浏览器中的资产；项目和材质包下载在经路径检查的测试临时目录中完成后清理。

## 面料与图案交互

- `design-workspace.js`：底栏同级分类、资产库选择弹窗、精简材质栏及其 UI 状态。
- `shared/material-placement.js`：材质大类兼容、UV 偏移/角度校验、映射矩阵叠加。
- 材质资产使用 `materialType: fabric | pattern`；旧数据默认 fabric。该字段随 IndexedDB 和 .formmat 往返。
- 新图案实例使用 `singlePlacement`，调用局部曲面投影生成几何，贴图采用 ClampToEdgeWrapping。旧项目 UV 图案仍走原路径。
- .form 保存面料 placement、图案表面参数/偏移/投影点/角度及待放置图案包。
- 旧完整编辑控件在设计台作为隐藏且 inert 的兼容容器保留，用于现有项目恢复；可见 UI 仅提供场景灯光和四项材质设置。

## 项目文件清单

逐文件清单见 [文件清单.md](./文件清单.md)，包括入口、模块、第三方库、测试与截图。

根目录包含 `index.html`、`material-editor.html`、`asset-library.html`、`app.js`、`style.css`、`workspace.css`、`server.ps1`、`启动编辑器.bat`、`使用说明.md`、`入口架构.md`、`package.json`、`MM06-展厅版.fbx`、`test.txt`；功能模块包括 `material-assignments.js`、`physical-textures.js`、`image-density.js`、`uv-pattern.js`、`surface-pattern.js`、`material-reveal.js`；入口与共享代码位于 `entries/` 和 `shared/`；品牌图片位于 `assets/`；Three.js 及插件位于 `vendor/three/`；回归测试与验证产物位于 `checks/`。

`server.ps1` 会在本地启动静态服务并按扩展名返回 HTML、JavaScript、CSS、图片和 FBX。三个页面都由这个同一服务提供，因此资产库数据和入口导航使用同一来源。

## 拖拽与共享顶栏

图案底栏复用 app.js 中面料的指针捕获、长按预览和拖拽状态；source 放置新实例，placed 移动既有实例。material-reveal.js 提供图案径向显现和共享 2200 ms 时长；临时动画材质在完成、删除、保存时释放。三个页面的品牌与导航由 shared/navigation.js 统一，header.css 覆盖统一浮动玻璃样式及居中布局。
