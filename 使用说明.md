# Spenic 工作空间

双击「启动编辑器.bat」，浏览器会自动打开编辑器。无需安装 Node.js 或 Python，无需联网。请保持启动器窗口打开；关闭窗口即停止本地服务。建议使用最新版 Edge 或 Chrome 并启用硬件加速。

## 三个独立入口

- **设计台**：`index.html`，默认首页。载入 FBX、拖放替换材质、布置图案、编辑灯光、保存 `.form` 项目和导出图片。
- **材质编辑器**：`material-editor.html`。无需载入展厅模型即可创建材质，配置七类 PBR 贴图、表面参数和厘米尺寸；保存到资产库或导出 `.formmat` 材质包。面料使用材质球/平面预览，默认手动厘米尺寸，可点击读取贴图 DPI。图案使用二维尺寸预览，自动从基础颜色贴图 DPI 换算宽高。
- **资产库**：`asset-library.html`。顶部切换面料库、图案库和模型库；独立图片归入图案库，面料可按分类筛选。卡片整体为 4:3，间距 8px。模型自动生成预览图，通过“设计”进入设计台；面料、图案通过“编辑材质”打开编辑器。右上角“···”提供重命名、编辑简介、替换预览图、导出和删除；自定义预览图最大 16 MB。材质包导出包含简介和自定义预览图。FBX 与配套图片一起选择可一并保存纹理资源。

三个页面顶部均可切换入口。材质编辑器中未保存的修改会在离开时提醒。设计台切换页面前，请先保存当前 `.form` 项目。

典型流程：在材质编辑器选择“面料”或“图案”，编辑并保存 → 返回设计台，切换底栏同级的“面料 / 图案” → 点击列表末尾 ＋ → 在资产库弹窗中选择材质。面料新增按钮为圆形，图案新增按钮为方形，紧随最后一个材质。底栏使用左右箭头翻页，内容一页放得下时不显示箭头，到达首尾时隐藏对应方向。弹窗按当前大类筛选，支持搜索、刷新、关闭和 Esc。

面料按原有物理尺寸或 UV 重复平铺，添加后拖动底部材质球到模型表面。面布、边布、包边条仍是面料子分类。旧版未设置大类的材质默认归入面料。

图案需要基础颜色图片，按材质编辑器中设置的厘米宽高贴在模型局部表面，不重复平铺；支持透明 PNG 和配套 PBR 贴图。添加后直接从底栏拖到模型表面放置；拖动已贴合图案可移动位置，单击仅编辑属性。长按放大预览，拖动时缩小跟随，悬停模型时显示心跳提示；拖到空白处或按 Esc 取消。已贴合图案可选择或移除；UV 偏移每增加 1，相当于沿投影平面移动一张图案的宽或高。新增图案采用局部曲面投影，避免重叠 UV 导致重复出现；旧项目已有 UV 图案保持原映射。

材质编辑器切换到“图案”后，上传或替换基础颜色贴图会自动读取图片内的 DPI，并按“像素 ÷ DPI × 2.54”计算宽高（cm）；先上传图片再切换到图案也会自动换算。其他 PBR 通道不参与图案尺寸计算。没有有效 DPI 时保留当前宽高并提示手动填写，不假定图片为 72 或 96 DPI。左侧为二维画板，滚轮以鼠标位置为中心缩放，拖动平移，按 F 恢复适应画板的默认视角（输入框中打字不触发）。上方和右侧的 cm 尺寸线只测量非透明图案的边界，排除透明留白；缩放不改变物理尺寸，旋转后重新测量可见范围，全透明图像不显示尺寸线。右侧宽高仍表示完整贴图尺寸，以保持 DPI 和模型铺贴比例一致。保存和重新打开材质会保留已确认的尺寸，包括手动调整。

进入设计台时属性栏默认折叠，点击视图右上方“属性”可展开场景与灯光。单击底栏材质或模型表面后展开材质栏，只显示预览、基础颜色、UV 偏移 U/V 和角度；点击“← 场景与灯光”返回。设计台不再显示 PBR 上传、分类和贴图尺寸等完整编辑控件，这些在独立材质编辑器中设置。

面料偏移与角度叠加于原有 UV / 物理尺寸映射；新增面料、待放置图案、已贴合图案及其颜色、位置、角度均随 .form 项目保存。

资产保存在当前浏览器、当前本地服务地址的 IndexedDB 中，不会写回项目文件夹。更换浏览器、端口或清除站点数据后，资产不会自动迁移；请通过资产库导出重要资源备份。材质包 `.formmat` 与完整项目 `.form` 分别从资产库和设计台导入。

底栏右侧垃圾桶居中显示，可拖入材质或图案直接删除；点击进入多选删除模式，上方为“取消”，下方为“删除”。正在应用的面料不可删除，未应用面料可选；图案无论待放置、已应用来源还是已贴合均可删除，删除已贴合项会同时移除模型上的图案，删除来源项不影响其已有贴合项。可跨页选择，点击取消或 Esc 退出；删除结果随 `.form` 项目保存，资产库中的原始资产保留。

模型默认缩放为 1.2；模型自带材质归入新增的“其他”分类，面布、边布、包边条分类继续用于新增面料。

## 设计台使用

- 默认载入 MM06 展厅模型；导入新模型可拖入 FBX 与配套纹理，或从资产库模型卡片进入。
- 模型和渲染资源准备好后，播放 1 秒开场拉远：从默认相机距离的 55% 出发，先快后慢到达默认视角。拖动或缩放可接管相机；打开已有 `.form` 项目时保留项目保存的视角。
- 右上方工具组左侧的展厅图标可进入展厅模式：自动旋转并请求全屏，清晨、黄昏、夜晚按“过渡 5 秒、停留 3 秒”循环。顶栏向上、底栏向下滑出，工具组收拢隐藏，展厅退出按钮移至右侧属性按钮位置。再次点击该按钮或按 Esc 退出，恢复进入前的光效、旋转和属性栏状态；退出浏览器全屏也会退出展厅模式。浏览器拒绝全屏时可使用 F11。系统启用“减少动态效果”时跳过镜头和界面动画，光效直接切换。
- 左键旋转，滚轮缩放，中键或右键平移；F 键适应视图。
- 面料拖放替换目标材质槽，底栏“撤销换材”恢复最近的替换。
- 顶部“保存项目 / 打开项目”存取 .form，导出图片生成当前视角 PNG。
- 属性编辑器的“环境场景”中，填写场景名称并点击“保存场景”，下载独立的 `.formscene` 文件；以后点击“载入场景”即可复用。文件仅记录环境预设、背景颜色、曝光、环境反射强度、主光强度/方向/颜色、补光强度、阴影、地面反射、地台和辅助网格，不包含模型、材质、贴图、图案或相机视角，也不会覆盖当前模型的旋转、缩放和材质。场景文件可跨模型使用；完整作品仍用 `.form` 项目保存。
- 模型图案可从底栏拖拽重新定位或移除；Esc 取消拖拽。
- 页面打开时默认应用已保存的清晨场景。视图右上方的纯图标按钮按清晨 → 黄昏 → 夜晚 → 清晨循环，使用内置的三份已调好场景。同一个太阳逐渐下沉为落日、收拢光芒并弯成月牙，再舒展回太阳；图形与场景共用约 2.6 秒过渡，主光沿同一方向绕行，背景、曝光、光色、补光和阴影同步渐变。连续点击可接着当前画面切换；手动调整环境或载入场景会接管设置。系统启用“减少动态效果”时直接切换。

## 文件说明

index.html / style.css / app.js / material-assignments.js / physical-textures.js / image-density.js 是网页；vendor/three 是本地 Three.js r170 依赖与许可证；server.ps1 是本地静态服务。请整体移动文件夹，不要只移动 HTML。服务仅监听本机 127.0.0.1，默认端口 4186，端口占用时自动选择后续端口。

网页也可以部署到普通 HTTP/HTTPS 静态网站空间：保持所有 HTML、CSS、JavaScript 文件，以及 entries、shared、assets、vendor 文件夹的相对目录完整，并附带可选示例 FBX。当前交付为本机浏览器版本，未发布到公网；上传操作仅在浏览器内处理，不发送至云服务。

FBX 显示依赖文件本身的几何、UV 和贴图。参考图片提供视觉方向；现有 FBX 的造型和细节保持原样，不是从单张图片精确重建。该示例 FBX 有 9 个材质、约 90.7 万三角面，引用了未随文件提供的外部纹理，可从编辑器补充。FBX 原材质转换为标准 PBR，专有着色器、程序材质和复杂渲染节点不保证与原 DCC 完全一致。当前不提供网格雕刻、动画编辑或 FBX 导出。


## 按真实物理尺寸铺贴

在独立材质编辑器中填写厘米宽高。面料默认手动指定尺寸，贴图记录了 DPI 时可点击“从基础颜色贴图读取 DPI”；图案上传基础颜色贴图后自动读取并换算。公式为“厘米 = 像素 ÷ DPI × 2.54”。没有 DPI 的图片使用手动尺寸，不推测图片的物理大小。

- 颜色与自发光使用 sRGB，其余 PBR 通道为线性数据；材质编辑器的粗糙度贴图按 RGB 灰度读取，金属度 B、AO R。
- 面料按物理尺寸重复铺贴，各贴图需代表同一实物范围；图案按宽高投射一次，不平铺。
- 面料真实尺寸依赖 FBX 单位、UV 密度和模型尺寸正确；原 FBX 内置烘焙图保留原映射。
- 项目保留已有单位校准和旧项目映射信息。模型缺失或退化 UV 时，面料应先在建模软件中修复；新增局部投影图案不依赖原模型 UV。

## 统一顶栏与动效

设计台、材质编辑器、资产库共用浮动玻璃顶栏；导航在顶栏居中，操作按钮保持在右侧。窄屏采用两行布局保持居中且避免重叠。面料与图案的径向晕染持续约 2.2 秒；系统开启减少动态效果时直接完成应用。

## 材质编辑器布局与上传

右栏顺序为顶部“面料 / 图案”切换、名称与面料分类、铺贴尺寸、贴图。每个通道左侧是贴图预览与上传，右侧是对应数值输入和滑条；基础颜色、自发光颜色支持色值输入。法线行提供 Y 翻转，粗糙度行提供“黑白反转”：黑色更光滑、白色更粗糙，开启后黑白互换。粗糙度按 RGB 灰度（0.299R + 0.587G + 0.114B）采样，滑条为灰度的强度倍率；缩略图同步显示灰度和反转结果。原始图片保持完整，灰度与反转设置随资产、材质包和项目保存，并在设计台使用相同的渲染方式。图案模式隐藏预览标题、DPI 说明和贴图区说明，仅保留画板及编辑控件。

可点击贴图区选择文件，或将一张 PNG/JPG/WebP/BMP 拖到对应行，拖入时高亮提示；每张最多 64 MB。无效文件不覆盖已有贴图，贴图右上角 × 可移除。


日光切换按钮位于模型视图左下角。右侧工具栏的旗帜按钮开启标记模式，同时自动展开标记属性栏；点击模型表面可添加标记。开启时旗帜呈绿色呼吸高亮，可见度在 50%～100% 之间变化。属性栏可切换已有标记、编辑信息和删除标记；模型上的标记仅展示信息，点击圆点可回到对应属性。标记随 `.form` 项目保存。标记模式期间自动旋转暂时停止，旋转图标取消高亮。再次点击旗帜或按 Esc 会退出模式并收起标记栏；点击底部材质球或图案时退出标记模式，属性栏保持展开，直接切换为所选材质内容。退出后恢复原有旋转设置，旋转实际恢复时图标重新点亮；拖动模型查看视角不会新增标记。

标记属性栏采用逐条列表，每条最右侧的 × 删除该条，铅笔按钮进入编辑。新标记的模型浮框默认留空并自动聚焦，输入后点击右侧 √ 或按 Enter 保存；中文输入法确认候选时不会提前保存。新标记未保存时，点击浮框外的内容、拖动或缩放视角、切换视角及退出标记模式都会直接取消该标记，已输入的文字也不会保留。保存后的模型浮框只展示信息，后续修改均在列表中进行，同样通过 √ 或 Enter 确认。

模型旋转、平移、缩放及视角切换时，所有模型标记统一隐藏；鼠标松手后立即恢复未被遮挡的标记，不等待惯性运动完全停止；惯性期间继续更新标记位置与遮挡状态。遮挡结果在静止时复用，模型使用独立空间索引加速检测，避免标记增多后反复扫描完整模型。标记确认按钮使用圆圈勾图标，不附带文字；列表上方仅保留属性栏标题。

“属性”作为独立按钮位于“标记模式”右侧，点击后按钮本身在约 0.56 秒内变形展开为属性栏，面板内容随展开逐渐显现，类似 app 图标打开页面。点击属性栏右上角的收起按钮，面板缩回原来的属性按钮。桌面视图中的模型随同一动画进度向左平移，收起时回到原位。开启系统减少动态效果时直接切换。

## 开发者模式

鼠标停在左上角 Spenic 标志图片上，连续输入 `admin`，可显示或隐藏品牌右侧的开发者按钮（相邻按键间隔不超过 2 秒）。点击按钮打开左侧开发者栏；再次输入 `admin` 隐藏入口时也会关闭面板。输入框编辑和中文输入法组字期间不会触发。刷新页面后入口默认隐藏。

状态栏显示页面状态、当前模型、日光时段、FPS、JS 堆内存估算、CPU 帧处理耗时、GPU 渲染耗时和绘制资源数量。CPU 是本页动画帧内的更新与渲染提交耗时，GPU 使用 WebGL 异步计时；均不是整机占用率。浏览器不支持的指标会明确标注，内存数据不包含显存。关闭面板后停止性能采样并释放计时查询。

在“日光流转 · 场景灯光”中选择清晨、黄昏或夜晚，点击“载入”预览对应场景；调节背景、曝光、环境反射、主光、补光和地台开关后，点击“将当前灯光保存到所选时段”。三个时段独立保存，普通日光切换与展厅循环共用这些设置，刷新后自动应用已保存的清晨。过渡中不能保存中间态；展厅运行中保留状态监控，退出展厅后再编辑灯光。

“展厅模式 · 日光时间”支持保存每次过渡时长（0.1–120 秒）和完成后的停留时长（0–600 秒），默认分别为 5 秒和 3 秒。运行中保存的新时长从后续过渡或停留开始使用，已开始的计时保持原时长。减少动态效果开启时省略过渡动画。普通手动日光切换仍使用约 2.6 秒。

灯光与时间保存在当前浏览器、当前网站地址的本地存储中；更换浏览器、端口或清理网站数据后不会自动迁移。可通过“导出配置”下载包含三个已保存时段和展厅时间的 JSON，通过“导入配置”校验并保存到其他浏览器，再载入时段应用。配置文件不包含模型、材质或开发者入口的可见状态，独立于项目文件。
