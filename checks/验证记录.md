# 验证记录

- JavaScript 主程序与本地 Three.js 模块通过语法检查。
- 本地 PowerShell 服务启动成功，健康检查与网页 HTTP 返回 200。
- 实际 MM06 FBX 在浏览器加载成功：2 个网格、9 个材质、约 90.7 万三角面。
- 浏览器渲染日志未发现错误或警告。
- 基础颜色上传显示预览并仅更新选中材质，其他材质的贴图数保持不变。
- 曝光滑块输入 1.55，数值与显示同步。
- .form ZIP 打包/解包数据往返检查通过。
- 浏览器恢复 .form 项目通过：模型名称、材质名称、基础颜色贴图、粗糙度 0.34、UV 重复 2、曝光 0.75 均恢复；其余 8 个材质无贴图。
- 贴图移除后预览与材质贴图计数清零；模型适应视图操作正常。
- 重复启动 PowerShell 服务正常退出并复用现有端口。
- 浏览器项目保存完成提示正常；内嵌浏览器未回传下载事件，因此不将下载文件落盘列为已验证项。
- 新增材质拖放回归测试：单材质/多材质网格替换、共享材质数组隔离、PBR 贴图保留、材质使用计数、保存/恢复分配、旧项目兼容和无效分配拒绝均通过。
- 中键平移回归测试通过：摄像机与观察目标同步位移，视角方向和观察距离保持不变；滚轮仍能缩放。
- 实际浏览器拖放通过：将第一颗深色材质球拖到床垫顶面，命中 uniform_1055 材质槽，使用数从 1 增为 2；原材质使用数变为 0，其他材质使用数不变；撤销按钮启用。
- 物理尺寸测试通过：200×100 cm 平面、20×10 cm 贴图铺贴为 10×10 次；不同网格变换、不同 UV 密度、米/厘米单位转换、预览归一化、矩形旋转、镜像 UV 与无效 UV 检查均通过。
- 浏览器实际 FBX 读取尺寸为 183.33×59.64×205.00 cm；上传测试贴图使用物理尺寸渲染，无 WebGL 错误。
- 宽度 10→20 cm、固定比例时高度同步至 20 cm；解锁后可设为 20×10 cm；45° 旋转正常。
- .form 物理尺寸项目恢复通过：20×10 cm、45°、物理尺寸模式、上传贴图，以及最长边 410 cm 的模型单位校准均恢复。
- 图片密度解析测试通过：PNG pHYs、JPEG JFIF/EXIF（大小端 TIFF、冲突时 EXIF 优先、旋转）、BMP、WebP、厘米单位、零值/缺失/截断数据，以及手动 DPI fallback。
- 浏览器自动 DPI 测试通过：128×128 px、300 DPI 自动计算为 1.0837×1.0837 cm，显示像素、双轴 DPI 和 PNG pHYs 来源。
- 无 DPI PNG 不会套用默认尺寸或覆盖材质；显示待应用状态。指定 300×300 DPI 后正确换算并应用。
- .form 项目恢复自动 DPI 通过：贴图、300 DPI、1.0837 cm 实物尺寸与 30° 旋转均恢复，浏览器无错误。
