# FORM 材质展厅

双击「启动编辑器.bat」，浏览器会自动打开编辑器。无需安装 Node.js 或 Python，无需联网。请保持启动器窗口打开；关闭窗口即停止本地服务。建议使用最新版 Edge 或 Chrome 并启用硬件加速。

## 使用

- 默认加载当前目录的 MM06-展厅版.fbx。点击模型名称旁边的 ＋ 或右侧「选择 FBX 与配套纹理」可上传新模型，也可以拖入中间视图。FBX 的外部纹理可以和 FBX 一起多选导入，按文件名匹配。
- 左键拖动旋转，滚轮缩放，按住鼠标中键拖动或右键拖动平移视角。F 键适应模型；顶部工具提供正视、俯视、自动旋转、网格和全屏。
- 底部显示 FBX 实际材质列表，按原材质身份区分；同名但不同身份的材质保留为不同项目。双击模型表面也能选择对应材质。
- 把底部材质球拖到模型表面，松开即替换命中的网格材质槽（该槽对应的全部面一起替换）。拖动时显示目标材质提示；拖到空白处不会替换。材质的 PBR 贴图和参数一并应用。使用同一材质球的部位会同步响应后续材质编辑，未使用的原材质球仍保留，可拖回去恢复。
- 点击底部「撤销换材」撤销最近一次拖放，最多记录 30 次。保存项目会保留材质分配；旧项目仍可打开。
- 选择材质后，右侧分别上传基础颜色、法线、粗糙度、金属度、AO、凹凸高度、自发光贴图。支持 PNG/JPG/WebP/BMP，单张上限 64 MB。点击贴图右上角 × 移除。贴图需要模型具有 UV。
- 可编辑基础颜色、粗糙度、金属度、贴图强度、贴图实际宽高（厘米）、固定比例、纹理旋转与法线 Y 翻转。颜色和自发光为 sRGB，其余贴图为线性数据；粗糙度 G、金属度 B、AO R 通道（普通灰度图也适用）。凹凸贴图不改变网格轮廓。
- 「仅显示此材质」方便识别部件；「重置材质」恢复该材质的 FBX 初始状态。
- 场景提供柔光展厅、日光、深色影棚，支持曝光、环境反射、主光方向/颜色/强度、补光、阴影、地面反射、地台、模型旋转缩放与线框模式。
- 「保存项目」下载 .form 文件，包含原始 FBX、配套纹理、后续上传贴图、材质参数、相机和场景设置。「打开项目」恢复编辑。未保存的修改不会在刷新后保留。
- 「导出图片」导出当前模型视角 PNG（不包含编辑器界面）。

## 文件说明

index.html / style.css / app.js / material-assignments.js / physical-textures.js / image-density.js 是网页；vendor/three 是本地 Three.js r170 依赖与许可证；server.ps1 是本地静态服务。请整体移动文件夹，不要只移动 HTML。服务仅监听本机 127.0.0.1，默认端口 4186，端口占用时自动选择后续端口。

网页也可以部署到普通 HTTP/HTTPS 静态网站空间：上传 index.html、style.css、app.js、material-assignments.js、physical-textures.js、image-density.js、vendor 文件夹和可选示例 FBX。当前交付为本机浏览器版本，未发布到公网；上传操作仅在浏览器内处理，不发送至云服务。

FBX 显示依赖文件本身的几何、UV 和贴图。参考图片提供视觉方向；现有 FBX 的造型和细节保持原样，不是从单张图片精确重建。该示例 FBX 有 9 个材质、约 90.7 万三角面，引用了未随文件提供的外部纹理，可从编辑器补充。FBX 原材质转换为标准 PBR，专有着色器、程序材质和复杂渲染节点不保证与原 DCC 完全一致。当前不提供网格雕刻、动画编辑或 FBX 导出。


## 按真实物理尺寸铺贴

新上传的 PBR 贴图默认使用「真实物理尺寸」和「自动读取像素与 DPI」。编辑器读取图片文件中的像素尺寸与 DPI，并按 `厘米 = 像素 ÷ DPI × 2.54` 计算整张图片覆盖的实物范围，不必手调 UV 重复。例如 3000 px、300 DPI 的图片宽度为 25.4 cm，在 203.2 cm 的 1:1、无拉伸 UV 表面上重复 8 次。

- 支持 PNG `pHYs`/`eXIf`、JPEG JFIF/EXIF、BMP pixels-per-meter 和 WebP EXIF 密度信息；分别读取水平与垂直 DPI。若 JPEG 的 EXIF 与 JFIF 不一致，采用 EXIF 并在界面提示。
- DPI 必须由文件真实记录。程序不会把浏览器常见的 72 或 96 DPI 当作缺失文件的默认值。没有有效 DPI 的图片会停在待应用状态，不覆盖当前贴图；填写供应商或扫描仪提供的水平/垂直 DPI 后才会应用。仅凭像素值无法推导物理尺寸。
- 页面会显示文件名、像素尺寸、水平/垂直 DPI、元数据来源和换算后的厘米尺寸。自动模式下厘米宽高只读；可切换到「手动指定厘米（兼容）」覆盖元数据。
- 同一材质的颜色、法线、粗糙度、金属度、AO、凹凸和自发光分别读取各自文件的像素与 DPI。不同分辨率的 PBR 图如代表同一实物范围，应在各自文件中记录相应 DPI。纹理旋转角度由材质统一控制。
- FBX 中原有烘焙图集保持原有映射；自动物理尺寸控制应用于本编辑器上传的贴图。
- 使用原有 UV 方向和接缝，通过网格表面与 UV 的尺度关系标定厘米。不会按屏幕大小或展厅取景比例计算。拖拽同一材质到另一网格时，自动使用目标网格的尺度。
- 在「模型实际尺寸 · 核对单位」查看从 FBX 单位计算的 X/Y/Z 尺寸。若导出的单位错误，输入模型已知最长边（cm）并点击校准。相机缩放以及场景里的「预览缩放」均不会改变实物尺寸。
- .form 项目保存贴图宽高、锁定比例、角度、铺贴方式及模型校准。旧项目默认保留原 UV 模式，避免打开后改变原有外观；可主动切换为物理尺寸。
- 准确的尺寸依赖模型单位正确、UV 等比例且无明显拉伸。程序按每个网格的材质绘制范围测量表面与 UV 的平均比例；局部尺度不均或倾斜严重时会显示提示，不能据此保证每个花纹都严格等尺寸。缺失或退化 UV 的部位需要先在建模软件修复。
